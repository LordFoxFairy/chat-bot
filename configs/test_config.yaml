# 测试配置 - 使用真实模块互相验证

modules:
  # TTS - Edge TTS（不需要 API key）
  tts:
    adapter_type: "edge_tts"
    config:
      voice: "zh-CN-XiaoxiaoNeural"
      rate: "+0%"
      volume: "+0%"

  # ASR - FunASR SenseVoice
  asr:
    adapter_type: "funasr_sensevoice"
    config:
      model_dir: "models/asr/SenseVoiceSmall/iic/SenseVoiceSmall"
      device: "cpu"

  # VAD - Silero VAD (暂时禁用 - GitHub API 限流)
  # vad:
  #   adapter_type: "silero_vad"
  #   config:
  #     threshold: 0.5
  #     sample_rate: 16000

  # LLM - 暂时不测试（需要 API key）
  # llm:
  #   adapter_type: "langchain"
  #   config:
  #     model: "gpt-3.5-turbo"

  # Protocol - WebSocket
  # protocols:
  #   adapter_type: "websocket"
  #   config:
  #     host: "0.0.0.0"
  #     port: 8765
