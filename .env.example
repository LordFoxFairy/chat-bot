# ============================================================
# Chat-Bot 环境变量配置
# ============================================================
# 使用说明：
# 1. 复制此文件为 .env：cp .env.example .env
# 2. 填入实际的 API Key
# 3. 重要：请勿将 .env 文件提交到版本控制！
# ============================================================

# ==================== API 配置 ====================

# 统一 API Key（必填）
# 支持 OpenAI、Anthropic、DeepSeek 等，根据 config.yaml 中的 model_name 自动选择
API_KEY=your-api-key-here

# 可选：自定义 Base URL（用于 OpenRouter、OneAPI 等代理服务）
# BASE_URL=https://openrouter.ai/api/v1

# ==================== 应用配置（可选）====================

# 日志级别：DEBUG, INFO, WARNING, ERROR, CRITICAL
# LOG_LEVEL=INFO

# 服务器配置（默认使用 config.yaml 中的配置）
# CHATBOT_HOST=0.0.0.0
# CHATBOT_PORT=8765

# 自定义配置文件路径
# CHATBOT_CONFIG=backend/configs/config.yaml

# 模型缓存目录（默认 .cache/models）
# CHATBOT_CACHE_DIR=.cache
